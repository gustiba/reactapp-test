pipeline {
    agent any

    environment {
        CI = true
    }

    stages {
        // stages pertama
        stages ('Install depedencies react project'){
            steps{
                echo "Start install depedencies react project"
                sh "npm install"
            }
        }
        
    
    }
}
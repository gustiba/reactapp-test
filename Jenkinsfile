pipeline {
    agent any

    environment {
        CI = true
    }

    stages {
        // stages pertama
        stage ('Install depedencies react project'){
            steps{
                echo "Start install depedencies react project"
                sh "npm install"
            }
        }

        // stages kedua
        stage ('Test Project')
            steps{
                sh
            }
    }
}